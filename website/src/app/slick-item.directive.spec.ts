import { SlickItemDirective } from './slick-item.directive';

describe('SlickItemDirective', () => {
  it('should create an instance', () => {
    const directive = new SlickItemDirective();
    expect(directive).toBeTruthy();
  });
});
