ng serve --host 0.0.0.0

rem connect from mobile device connected to the same network
rem you can see the address of your server with 'ipconfig' command on Windows
rem the address is *.*.*.*:4200, where *.*.*.* is the IPv4 Address seen at ipconfig